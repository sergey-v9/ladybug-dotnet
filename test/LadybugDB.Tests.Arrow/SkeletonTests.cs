using Xunit;

namespace LadybugDB.Tests.Arrow;

/// <summary>Skeleton so the Arrow test project builds and runs. WS-E adds the Apache.Arrow
/// RecordBatch/ingest/CSR tests (native-gated where they touch the engine).</summary>
public sealed class SkeletonTests
{
    [Fact]
    public void Harness_IsWired()
    {
        Assert.True(true);
    }
}
