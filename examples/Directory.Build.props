<Project>

  <PropertyGroup>
    <!-- Published package version used by the examples. Override with:
         dotnet run -p:LadybugVersion=<version> -->
    <LadybugVersion Condition="'$(LadybugVersion)' == ''">0.17.1.0</LadybugVersion>
  </PropertyGroup>

</Project>
