<Project>

  <PropertyGroup>
    <!-- Published package version used by the examples. Override with:
         dotnet run -p:LadybugVersion=<version> -->
    <LadybugVersion Condition="'$(LadybugVersion)' == ''">0.19.1</LadybugVersion>
  </PropertyGroup>

</Project>
