using Xunit;

namespace LadybugDB.Benchmarks.Tests;

/// <summary>Skeleton so the benchmark-tests project builds and runs without the native engine.
/// WS-J adds the tests that validate the differential benchmark runner / CI gate logic.</summary>
public sealed class SkeletonTests
{
    [Fact]
    public void Harness_IsWired()
    {
        Assert.True(true);
    }
}
